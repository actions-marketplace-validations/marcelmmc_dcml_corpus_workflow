name: creating_docker_img

on: workflow_dispatch

jobs:
  perform_creation_of_image:
    runs-on: ubuntu-latest

    steps:

    - name: config git and delete old update_modules branch
      continue-on-error: true
      run: |
        git config --global user.name "marcel41"
        git config --global user.email marcel_moran41@hotmail.com
        git config --global user.token ${{ secrets.BOTSECRETCELM }}
      env:
          GITHUB_TOKEN: ${{ secrets.BOTSECRETCELM }}

      - name: Checkout main
        uses: actions/checkout@v3
        with:
          repository: marcelmmc/data_reports
          path: main
          token: ${{ secrets.BOTSECRETCELM }}

      # - name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v2
      #
      # - name: Login to Docker Hub
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_TOKEN }}
      #
      #
      #
      #
      # - name: Build and push
      #   uses: docker/build-push-action@v4
      #   with:
      #     context: "{{defaultContext}}:creating_docker_image"
      #     push: true
      #     tags: ${{ secrets.DOCKERHUB_USERNAME }}/dockerms3:testimg
